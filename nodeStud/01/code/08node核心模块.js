// 获取机器信息
var os = require('os');

// 用来操作路径
var path = require('path');

//获取及其cpu信息
console.log(os.cpus());

// memory 内存
console.log(so.totalmem());

// extname 获取路径中的扩展名
console.log(path.extname('c:sdf/sdf/df/sdf/helow.txt'));